# Notebooks

Use this folder for Databricks notebook examples and step-by-step pipelines.

- Add a notebook that imports the `src` package and runs the pipeline functions.
- Keep the notebook focused on the Databricks workflow: mount S3, read raw data, transform, write Delta.
