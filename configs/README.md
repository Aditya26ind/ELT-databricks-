# Configurations

This folder contains example configuration and path definitions for your ELT project.

- `sample_paths.json` shows how to store S3 locations for raw, staging, and output data.
- Use these values in local development or to make Databricks job parameters configurable.
